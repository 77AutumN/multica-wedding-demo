# 开发约定

- 默认使用简体中文。范围为已批准的虚构婚宴 Demo；保留固定 Multica、Claude Code、Lark CLI 版本及专用非管理员执行身份。
- 复用现有工具与标准库。Commerce Builder 用于职责和行为验收，Ponytail 用于控制规模；插件不是运行依赖。
- 修改经分支、PR、离线检查、Codex 审查和必要实际验收。用户授权 Codex 自主合并与发布；关键验证失败不得发布成功标签。
- 正式运行的可信身份、固定资源、写前持久化、回读和未知结果保护不可因测试而放宽。六位码不是宿主强制审批或原子幂等。
- 仅按明确文件清单部署到现有运行目录；不覆盖现场凭据、确认码历史、数据库或备份，不修改旧 MYANC 项目。
- 不上传真实配置、个人标识、原始运行日志及对话、备份、二进制安装包。新增测试只用虚构数据，生成物放临时目录。
- 本地故障模拟与真实飞书代操作必须分开记录；真实记录脱敏后才进入仓库。
