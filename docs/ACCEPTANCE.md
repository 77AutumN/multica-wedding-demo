# 验收状态

状态：0.1.0 候选，尚未合并或发布正式标签。真实验收被 Claude 组织的订阅访问限制阻塞，已停止执行端并关闭写入。

- 22 项离线业务模拟通过；干净检出无需真实配置或登录工具即可运行。
- 故意破坏断言和放入虚构的凭据形状标识时，检查均正确失败；还原后重新通过。
- GitHub Windows PowerShell 5.1 CI 通过；只读权限，无现场凭据或自动部署。
- Owner 与 test 已分别通过飞书代操作验证真实身份。第一条样例经原人确认后创建并回读：28 桌、84,000 元。
- Owner 询价显示增加 9,000 元、合计 93,000 元；状态哈希、Base 版本与订单字段保持不变。
- 第一轮已从 28 桌改为 31 桌，其中 3 桌无海鲜套餐；金额 93,000 元、业务第 2 版，test 已接收。菜单与布桌均尚待落实，完整故事为 0/3 轮。
- 已通过真实代操作：纠正换码、旧码拒绝、跨人确认拒绝、全角小写确认、重复确认零写入、test 冒充销售被拒绝、销售不能代反馈。
- 销售代反馈拒绝曾夹带英文；补强 Agent 指令后，新会话同一请求回复为中文。查询及仅询价回归通过，状态未变化；原失败保留。
- 代码审查发现数字 0 与空白混同；共享比较函数已修正。补充缺失/null/空串和零值的模拟，修复前失败，修复后全部 22 组通过。该故障模拟没有制造真实 Base 故障。
- Claude 返回组织禁用订阅访问，取消菜单预览的 Run 在业务工具执行前失败。预览原件保留，发布标识变化使旧预览失效，恢复后须重新预览。
- 三轮完整故事、其余真实边界和正式发布尚未完成。当前备份恢复进度见本机桌面说明，不将早期 CRM 恢复结果写成本版通过。

真实飞书操作与本地模拟分别记录。原始消息、Run、成员、Base 标识和工具证据只保留在本机，仓库仅保存脱敏结论。
