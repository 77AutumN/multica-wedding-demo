# 验收记录

2026-09-19：最终业务稿 `f215249` 完成三轮真实飞书演示，均由 Codex 使用 computer use 代操作 Owner 和 test。完整备份的 Windows 提权启动被取消，备份助手没有开始；Bot已恢复并通过重启后的真实查询。候选尚未合并或创建正式标签，等待本版完整备份恢复。

## 三轮完整故事

| 项目 | 第一轮 | 第二轮 | 第三轮 |
|---|---|---|---|
| 新样例 | 独立编号、仅一条 | 独立编号、仅一条 | 独立编号、仅一条 |
| 初始订单 | 28桌、84,000元、第1版 | 同左 | 同左 |
| 查询与仅询价 | 加3桌为93,000元，增加9,000元；零写入 | 通过 | 通过 |
| 确认变更 | 31桌，其中3桌无海鲜套餐；93,000元、第2版 | 通过 | 通过 |
| test 接收与菜单反馈 | 已接收、菜单已落实，布桌仍待落实 | 通过 | 通过 |
| 查询遗漏 | 仅布桌待落实，负责人为 test | 通过 | 通过 |
| 单独确认布桌 | 两项已落实；仍为第2版 | 通过 | 通过 |
| 实际保存次数 | 4次 | 4次 | 4次 |

每轮的四次保存分别为创建样例、变更订单、接收及菜单反馈、布桌反馈。合并接收和明确分项反馈是已约定行为。最终实际 Base 快照含准备轮加三轮共四条订单，各编号只有一条，历史保留；当前没有待确认或待查证操作。三轮运行期间六个业务文件哈希一致。

每次保存都关联飞书消息、应用内真实发送者、Multica 发起人、预览/确认 Run、工具结果、操作号及全部字段回读。原始身份与日志仅在本机；此处不上传聊天、确认码或资源标识。

## 拒绝与异常验证

| 场景 | 证据类别与结果 |
|---|---|
| Owner/test 身份、销售不能代反馈、test 不能改订单或开新轮、自报老板无效 | 真实飞书代操作通过 |
| 纠正保留内容、旧码无效、越人确认、重复确认零写入、全角小写 | 真实飞书代操作通过 |
| 33桌超容量 | 最终稿真实拒绝，状态哈希不变 |
| 预览遇执行端重启/发布切换 | 真实旧码拒绝；离线模拟也通过；三轮后空闲重启查询和绑定仍正常 |
| 取消、错码、多码、否定/引用、确认夹带修改、短码碰撞 | 22组离线检查覆盖；纠正和旧码另有真实记录。早期一次取消因 Claude 认证中断未执行，不记为真实通过 |
| 非法桌数、未知菜单、禁止覆盖价格/资源、无变化零写入 | 离线模拟通过；超容量另有真实记录 |
| 旧版本反馈重置、外部冲突、损坏文件、保存失败、新轮次响应丢失 | 离线模拟通过；没有故意破坏真实数据或断电 |
| 实际网络 EOF | 未自动重写，进入待查证；三次只读回查后由维护者人工核查归档旧操作，再经飞书新预览和新确认成功 |

准备轮包含认证中断、网络人工核查和指令修复，不计入最终连续三轮。失败和复测均保留，详见 [改善记录](IMPROVEMENT.md)。六位码仍不是宿主强制审批或数据库原子幂等；实际网络故障的人工处置不等于自动恢复保证。

## 检查与协作

- 22组离线模拟通过；最终稿从干净目录取得，无现场配置、CLI或登录也能运行。
- 故意破坏断言、放入虚构凭据形状标识，检查均正确失败；还原后通过。
- GitHub Windows PowerShell 5.1 检查通过；没有现场凭据、自托管 runner 或自动部署。
- Codex 分别审查规格和代码规范；数字零与空白比较问题修复前断言失败、修复后通过，并完成复审。这不是两名独立人员审批。
- 仓库私有；第一轮没有新增协作者。只上传明确列出的源码和脱敏资料，不操作旧 MYANC 项目。

## 实测耗时

| 项目 | 三轮结果/范围 | 说明 |
|---|---|---|
| 查询＋询价执行 | 21.24 / 19.69 / 20.20秒 | 中位20.20秒，Run开始至完成 |
| 订单变更预览执行 | 13.83 / 14.09 / 13.10秒 | 中位13.83秒 |
| 12次保存执行 | 14.46—18.62秒 | 中位14.85秒 |
| 查询＋询价飞书可见等待 | 70.75 / 34.12 / 36.73秒 | 中位36.73秒，发送至首次观察到完整回复的上界 |
| 订单预览飞书可见等待 | 49.30 / 33.05 / 44.72秒 | 中位44.72秒，同为采样上界 |
| 完整故事墙钟时间 | 16分41秒 / 6分32秒 / 7分23秒 | 含账号切换、Codex操作与观察；第一轮另含容量拒绝及较长观察间隔 |

可见等待包含观察延迟，不能当成精确网络耗时或用户保证。本轮记录的是婚宴 Demo 的实际表现，不据此声称比旧 CRM 提速多少。

## 完整案例

销售查询当前订单并仅询价 → 28桌保持不变 → 提出31桌、新增3桌无海鲜套餐 → 工具生成完整旧新值和随机短码 → 原销售确认 → Base回读31桌/93,000元/第2版 → test接收并反馈菜单已交接、布桌未核对 → 原test确认 → 遗漏查询仅显示布桌及test → 单独确认布桌 → 回读两项完成，业务版本仍为2。

另有准备轮纠正案例：先预览30桌/2桌特殊菜单，再纠正至31桌/3桌；原短码失效，跨人确认被拒绝，原人使用新码保存，重复确认不再写入。该轮随后遇真实网络EOF，人工核查与重新预览均留有轨迹；最终交接完成。
